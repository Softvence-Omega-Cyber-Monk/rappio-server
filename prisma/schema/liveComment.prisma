model LiveComment {
  id        String   @id @default(uuid())
  userId    String
  videoId   String // or streamId
  message   String
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}
